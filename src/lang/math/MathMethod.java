package lang.math;

public class MathMethod {
    /*
    기본 연산 메서드
    abs(x) -> 절대값
    max(a, b) -> 최대값
    min(a, b) -> 최소값

    지수 및 로그 연산 메서드
    exp(x) -> e^x 계산
    log(x) -> 자연 로그
    log10(x) -> 로그 10
    pow(a, b) -> a의 b 제곱

    반올림 및 정밀도 메서드
    ceil(x) -> 올림
    floor(x) -> 내림
    rint(x) -> 가장 가까운 정수로 반올림
    round(x) -> 반올림

    삼각함수 메서드
    sin(x) -> 사인
    cos(x) -> 코사인
    tan(x) -> 탄젠트

    기타 유용한 메서드
    sqlt(x) -> 제곱근
    cbrt(x) -> 세제곱근
    random() -> 0.0과 1.0 사이의 무작위 값 생성
     */
}
